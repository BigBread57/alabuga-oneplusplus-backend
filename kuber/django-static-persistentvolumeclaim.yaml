apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: django-static
  name: django-static
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
